# Core AI
numpy>=1.26
torch>=2.2          # install manually with CUDA variant if on GPU:
                    #   pip install torch --index-url https://download.pytorch.org/whl/cu121

# Training
mlflow>=2.0
tqdm>=4.66

# Web server (UI)
fastapi>=0.111
uvicorn[standard]>=0.29
websockets>=12.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
